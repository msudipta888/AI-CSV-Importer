{
  "name": "groweasy-csv-importer-backend",
  "version": "1.0.0",
  "description": "Backend API for GrowEasy AI CSV Importer",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "npm run build",
    "test": "jest --testPathPatterns=src/tests --verbose",
    "test:coverage": "jest --testPathPatterns=src/tests --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "nodemon": "^3.1.0"
  }
}