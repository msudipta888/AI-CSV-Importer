name: Second ci

on:
   workflow_dispatch:


jobs:
     build:
         runs-on: ubuntu-latest
         steps:
            - name: say hello
              run: echo "Hello from github action"

            -  name: Install
               env: 
                APP_ENV: demo
               run: echo Environment $APP_ENV;