name: add env value

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "Hello from github action"

      - name: Install
        env:
          APP_ENV: dev
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: echo "Environment $APP_ENV set (GEMINI_API_KEY is configured)"